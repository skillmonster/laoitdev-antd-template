import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_admin/(Checkin-Out)/checkin-out/')({
  component: () => <div>Hello /_admin/(Checkin-Out)/checkin-out/!</div>
})