import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_admin/(Checkin-Out)/checkin-out-setting/')({
  component: () => <div>Hello /_admin/(Checkin-Out)/checkin-out-setting/!</div>
})