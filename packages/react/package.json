{
  "name": "react",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://souliya_pps.gitlab.io/template-antd",
  "scripts": {
    "dev": "vite",
    "build": "tsc && yarn run generate-routes && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "generate-routes": "tsr generate",
    "watch-routes": "tsr watch"
  },
  "dependencies": {},
  "devDependencies": {}
}